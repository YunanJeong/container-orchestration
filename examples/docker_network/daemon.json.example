# 실제 사용시 주석을 지우고 json파일 형태로 쓴다.

# 가용범위
# 하나의 호스트에서 4개의 도커 network   (4)
# 하나의 도커 network 당 61개 Container (64-3)
{
    "bip": "172.31.1.1/26",
    "default-address-pools":[
        {"base": "172.31.1.1/24", "size": 26}
    ]
}

# 가용범위
# 하나의 호스트에서 2개의 도커 network    (2)
# 하나의 도커 network 당 125개 Container (128-3)
{
    "bip": "172.31.1.1/25",
    "default-address-pools":[
        {"base": "172.31.1.1/24", "size": 25}
    ]
}

